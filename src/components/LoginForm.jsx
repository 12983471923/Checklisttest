import AuthLoginForm from './AuthLoginForm';

// Backwards-compatible export for archived/test screens. The legacy local
// username/password form was removed so credentials are never bundled.
export default AuthLoginForm;
